apiVersion: v1
kind: Service
metadata:
  name: {{ include "backend-fastapi.fullname" . }}
  labels:
    {{- include "backend-fastapi.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "backend-fastapi.selectorLabels" . | nindent 4 }}
