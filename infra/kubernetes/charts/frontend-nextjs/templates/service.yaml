apiVersion: v1
kind: Service
metadata:
  name: {{ include "frontend-nextjs.fullname" . }}
  labels:
    {{- include "frontend-nextjs.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "frontend-nextjs.selectorLabels" . | nindent 4 }}
